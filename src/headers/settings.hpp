#ifndef _SETTINGS_HPP_
#define _SETTINGS_HPP_

// whether or not to slow down the sorting algorithm at run time
const bool slowSort = false;

// amount of numbers in each array
const int arraySize = 200;

#endif